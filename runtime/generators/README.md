# Prompt Generator

## Purpose

The Prompt Generator combines Layer 0 Core files and resolved Layer 1 Domain Modules into a single canonical prompt payload.

## Scripts

- `generator.sh` — POSIX-compliant prompt assembly script.

## Usage

```bash
# Generate canonical prompt for python and security
./runtime/generators/generator.sh python security
```
